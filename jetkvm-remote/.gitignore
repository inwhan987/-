node_modules/
dist/
dist.tar.gz
release/
# android/ is committed on purpose -- it carries hand-written native code
# (JetKvmProxyServer.java, SettingsProxyPlugin.java) that `cap add` can't
# regenerate; android/.gitignore covers its own build artifacts.
ios/
*.log
.DS_Store
tsconfig.tsbuildinfo
